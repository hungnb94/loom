# Edge case: crash recovery — pipeline persists state before each node
entry: step1
steps:
  step1:
    type: shell
    commands:
      - 'echo "step1 done"'
    next: step2
  step2:
    type: shell
    commands:
      - 'echo "step2 done"'
    next: step3
  step3:
    type: shell
    commands:
      - 'echo "step3 done"'
